{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v uv >/dev/null 2>&1 || { echo '[glyphfit] uv not found on PATH — install from https://docs.astral.sh/uv/ to enable image shrinking' >&2; exit 0; }; UV_PROJECT_ENVIRONMENT=\"${CLAUDE_PLUGIN_DATA}/.venv\" uv sync --project \"${CLAUDE_PLUGIN_ROOT}\" --quiet",
            "timeout": 120
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "PY=\"${CLAUDE_PLUGIN_DATA}/.venv/bin/python\"; [ -x \"$PY\" ] || exit 0; \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_read_hook.py\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}

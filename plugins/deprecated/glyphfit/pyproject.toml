[project]
name = "glyphfit"
version = "0.1.0"
description = "On-demand text-aware image downscaling for Claude vision."
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.0.0",
    "rapidocr-onnxruntime>=1.4.0",
]

[tool.uv]
package = false
